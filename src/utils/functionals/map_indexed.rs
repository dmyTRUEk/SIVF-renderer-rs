//! Map Indexed is `.map(…)` but also with index ;)

// use std::{result::Iter, iter::Map};



// TODO: write it and use in sivf_objects/complex/gradient.rs
// pub trait ExtensionMapIndexed
// where
//     Self: Sized,
//     F: FnMut(Self::Item) -> R
// {
//     fn map_indexed(&self, f: F) -> Map<Self, F>;
// }
// impl<T, R> ExtensionMapIndexed for Iter<'_, T> {
//     fn map_indexed(&self, f: impl FnMut(T)) -> Vec<usize> {
//         self.enumerate().map(f)
//     }
// }

